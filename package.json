{
  "name": "focusflow",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:api": "turbo run dev --filter=@focusflow/api",
    "dev:mobile": "turbo run dev --filter=@focusflow/mobile",
    "dev:stop": "(lsof -t -i :3000 || true) | xargs -n1 kill -9 2>/dev/null || true && (lsof -t -i :8081 || true) | xargs -n1 kill -9 2>/dev/null || true && (lsof -t -i :8082 || true) | xargs -n1 kill -9 2>/dev/null || true",
    "dev:ios:all": "pnpm run dev:stop && pnpm --filter @focusflow/api dev & sleep 2 && pnpm --filter @focusflow/mobile exec expo start --dev-client --port 8081 --ios",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "prettier": "^3.3.3",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{md,json,yaml}": [
      "prettier --write"
    ]
  }
}
